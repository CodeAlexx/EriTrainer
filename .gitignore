/target
backend/**/target/
backend/**/output/
backend/**/cache/
backend/**/*.safetensors
**/*.rs.bk

# Repository docs and lockfiles are part of the trainer handoff. Keep local
# scratch notes explicitly named instead of ignoring all markdown globally.
*.local.md
*.scratch.md
*.tmp.md

# Training outputs (LoRAs, samples) stay local — never commit
output/

# Parity oracle dumps (large generated reference tensors) stay local
trainer/parity/**/*.safetensors
